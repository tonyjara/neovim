return {
	-- "ojroques/nvim-bufdel",
	-- config = function()
	-- 	require("bufdel").setup({
	-- 		next = "tabs",
	-- 		quit = false, -- quit Neovim when last buffer is closed
	-- 	})
	-- 	local keymap = vim.keymap.set
	--
	-- 	keymap("n", "∑", ":BufDel<cr>", { silent = true, noremap = true, desc = "Delete one buffer" })
	-- 	keymap("n", "<leader>bk", ":BufDelAll<cr>", { silent = true, noremap = true, desc = "Delete all buffers" })
	-- 	keymap(
	-- 		"n",
	-- 		"<leader>bi",
	-- 		":BufDelOthers<cr>",
	-- 		{ silent = true, noremap = true, desc = "Delete all buffers but current" }
	-- 	)
	-- end,
}
